import { describe, it } from 'vitest';

describe('Antigravity Gap Tracking', () => {
    describe('Mouse Wheel (BrowserMouseWheelTool)', () => {
        it.skip('scrolls down by the specified pixels');
        it.skip('scrolls up by the specified pixels');
    });

    describe('Screen Recording (HandleScreenRecording)', () => {
        it.skip('starts recording the viewport to an mp4 file');
        it.skip('stops recording and saves the artifact');
    });

    describe('Tool Budgets', () => {
        it.skip('enforces maxContextTokens limits on get-dom outputs');
        it.skip('enforces suggestedMaxToolCalls warnings');
    });
});
